// Placeholder — the real hero/landing page is built in a later task.
export function HomePage() {
  return (
    <div className="flex flex-col items-center gap-2 py-16 text-center">
      <h1 className="font-heading text-3xl font-semibold">Reactor Design Explorer</h1>
      <p className="text-muted-foreground">Pick a reactor above to get started.</p>
    </div>
  )
}
